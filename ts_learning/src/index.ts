import _ from 'lodash';

const newArr = _.chunk([3, 4, 5, 6, 7], 2);
const newArr1 = _.chunk(['e', 'g', 'y', 'u', 'g'], 2);

console.log(newArr);
console.log(newArr1);

console.log('1');

setTimeout(() => {}, 1000);
setInterval(() => {}, 1000);
